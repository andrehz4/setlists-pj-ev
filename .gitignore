# Sistema operacional
Thumbs.db
desktop.ini
.DS_Store

# Backups locais
*.zip
*.7z
*.tar
*.tar.gz

# Editores
.vscode/
.idea/
*.swp
*~

# Configuração local de ferramentas
.claude/

# Artefatos de auditoria a11y / lighthouse (regeneráveis)
node_modules/
lighthouse-*

# package.json / package-lock.json eram ignorados antes (so havia
# deps de dev a11y); agora sao versionados pq o News bot depende deles.
.dev.log

# Artefatos locais do pipeline story IG (mockups intermediarios)
tmp/

# Previews gerados pelo simulador do mock-ig (regeneraveis, nao versionar)
media/news/_preview/
mock-ig/_store.json

# clipe de show pesado (insumo do reels, baixado pelo baixa-clipe)
media/reel-clip/
