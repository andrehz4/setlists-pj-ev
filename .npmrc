; smartcrop-sharp declara peer dep de sharp em faixa antiga; o codigo
; roda igual no sharp 0.33. legacy-peer-deps faz o npm ci (local e CI)
; instalar do lockfile sem falhar no conflito de peer.
legacy-peer-deps=true
