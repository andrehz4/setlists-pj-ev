import React from "react";

// Placeholder. Quando o pipeline ganhar suporte a reels (media_type=REELS,
// video_url), o mock ja guarda em stories[]/feed[]; aqui vira o feed vertical.
export default function Reels() {
  return (
    <div className="reels-ph">
      <h2>Reels</h2>
      <p>Em breve. Quando o pipeline publicar reels (media_type=REELS), eles aparecem aqui num feed vertical de video.</p>
    </div>
  );
}
